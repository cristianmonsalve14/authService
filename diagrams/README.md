# Diagramas — authService

El diagrama ER de esta base de datos está centralizado en:

`infraestructura/diagrams/auth_er_model.puml` (+ `.png`)

Regenerar junto con los demás ER:

```powershell
java -jar $env:USERPROFILE\.plantuml\plantuml.jar -tpng "infraestructura\diagrams\*_er_model.puml"
```
